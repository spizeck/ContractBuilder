rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Simple rule: any authenticated user can read/write everything
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
